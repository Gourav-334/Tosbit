MAKE TABLE PARSER:

-> Separate algorithms for "data type checking" & "row data insertion".
-> Re-read buffer from start for safety reasons.
-> Check the algorithm flow again & make modifications accordingly.
